apiVersion: v1
kind: Pod
metadata:
  name: pod-2
  labels:
    name: nginx
    # Pods must have this label set
    eno.io/pod-group-name: "test-podgroup"
  annotations:
    # Pods must have this annotation set
    eno.io/pod-group-name: "test-podgroup"
spec:
  schedulerName: eno-scheduler
  containers:
  - name: test
    image: nginx
    imagePullPolicy: IfNotPresent
