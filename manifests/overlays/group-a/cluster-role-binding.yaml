apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: godel
subjects:
  - kind: ServiceAccount
    name: godel
    namespace: godel-system
roleRef:
  kind: ClusterRole
  name: eno # 复用同一个 ClusterRole（权限相同）
  apiGroup: rbac.authorization.k8s.io
