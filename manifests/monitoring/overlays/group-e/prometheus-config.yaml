# ================================================================
# Prometheus config for Group E — Koordinator (Alibaba hybrid scheduler)
#
# Scrape targets:
#   • koord-scheduler (koordinator-system, HTTP port 10251)
#   • koord-manager   (koordinator-system, HTTP port 9316)
#
# Koordinator extends the kube-scheduler framework, so it exposes the
# standard "scheduler_*" metrics plus Koordinator-specific "koordinator_*"
# metrics. Key metrics:
#   scheduler_schedule_attempts_total{result}
#   scheduler_e2e_scheduling_duration_seconds
#   scheduler_scheduling_attempt_duration_seconds
#   scheduler_binding_duration_seconds
#   scheduler_framework_extension_point_duration_seconds
#   scheduler_plugin_execution_duration_seconds
#   koordinator_scheduling_*  (QoS-aware extensions)
# ================================================================
apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: monitoring
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s
      evaluation_interval: 15s

    rule_files:
      - "/etc/prometheus/rules.yml"

    scrape_configs:
      # ── Koordinator Scheduler ──
      - job_name: "koord-scheduler"
        kubernetes_sd_configs:
          - role: endpoints
            namespaces:
              names: ["koordinator-system"]
        relabel_configs:
          - source_labels: [__meta_kubernetes_service_name]
            action: keep
            regex: koord-scheduler-metrics
          - source_labels: [__meta_kubernetes_endpoint_port_name]
            action: keep
            regex: metrics
          - source_labels: [__meta_kubernetes_namespace]
            target_label: namespace
          - source_labels: [__meta_kubernetes_pod_name]
            target_label: pod
          - source_labels: [__meta_kubernetes_service_name]
            target_label: service

      # ── Koordinator Manager ──
      - job_name: "koord-manager"
        kubernetes_sd_configs:
          - role: endpoints
            namespaces:
              names: ["koordinator-system"]
        relabel_configs:
          - source_labels: [__meta_kubernetes_service_name]
            action: keep
            regex: koord-manager-metrics
          - source_labels: [__meta_kubernetes_endpoint_port_name]
            action: keep
            regex: metrics
          - source_labels: [__meta_kubernetes_namespace]
            target_label: namespace
          - source_labels: [__meta_kubernetes_pod_name]
            target_label: pod

  # ================================================================
  # Recording rules for Group E — Koordinator
  #
  # Since Koordinator extends kube-scheduler framework, it exposes
  # the same "scheduler_*" metric names as kube-scheduler, plus
  # custom "koordinator_*" metrics. We use the "koord:" prefix for
  # recording rules to distinguish from Group C.
  # ================================================================
  rules.yml: |
    groups:
      # ──────────────────────────────────────────────────
      #  Throughput
      # ──────────────────────────────────────────────────
      - name: koord_throughput
        interval: 15s
        rules:
          # Scheduling throughput (pods/s) by result
          - record: koord:scheduler_schedule_attempts:rate1m
            expr: rate(scheduler_schedule_attempts_total[1m])

          # Successful scheduling throughput
          - record: koord:scheduler_schedule_attempts_scheduled:rate1m
            expr: rate(scheduler_schedule_attempts_total{result="scheduled"}[1m])

          # Peak throughput
          - record: koord:scheduler_peak_throughput:30m
            expr: max_over_time(sum(rate(scheduler_schedule_attempts_total{result="scheduled"}[1m]))[30m:])

      # ──────────────────────────────────────────────────
      #  Latency
      # ──────────────────────────────────────────────────
      - name: koord_latency
        interval: 15s
        rules:
          # 单次调度尝试延迟 (Filter+Score+Bind) — scheduler_scheduling_attempt_duration_seconds
          - record: koord:scheduler_scheduling_attempt_duration:p50
            expr: histogram_quantile(0.50, sum by (le) (rate(scheduler_scheduling_attempt_duration_seconds_bucket[1m])))

          - record: koord:scheduler_scheduling_attempt_duration:p90
            expr: histogram_quantile(0.90, sum by (le) (rate(scheduler_scheduling_attempt_duration_seconds_bucket[1m])))

          - record: koord:scheduler_scheduling_attempt_duration:p99
            expr: histogram_quantile(0.99, sum by (le) (rate(scheduler_scheduling_attempt_duration_seconds_bucket[1m])))

          - record: koord:scheduler_scheduling_attempt_duration:avg
            expr: >
              sum(rate(scheduler_scheduling_attempt_duration_seconds_sum[1m]))
              /
              sum(rate(scheduler_scheduling_attempt_duration_seconds_count[1m]))

          # Pod 全周期 E2E 延迟 (入队 → Bound) — scheduler_pod_scheduling_sli_duration_seconds
          - record: koord:scheduler_pod_scheduling_sli_duration:p50
            expr: histogram_quantile(0.50, sum by (le) (rate(scheduler_pod_scheduling_sli_duration_seconds_bucket[1m])))

          - record: koord:scheduler_pod_scheduling_sli_duration:p90
            expr: histogram_quantile(0.90, sum by (le) (rate(scheduler_pod_scheduling_sli_duration_seconds_bucket[1m])))

          - record: koord:scheduler_pod_scheduling_sli_duration:p99
            expr: histogram_quantile(0.99, sum by (le) (rate(scheduler_pod_scheduling_sli_duration_seconds_bucket[1m])))

          - record: koord:scheduler_pod_scheduling_sli_duration:avg
            expr: >
              sum(rate(scheduler_pod_scheduling_sli_duration_seconds_sum[1m]))
              /
              sum(rate(scheduler_pod_scheduling_sli_duration_seconds_count[1m]))

          # Core scheduling algorithm latency (Filter+Score only, excludes Bind)
          - record: koord:scheduler_scheduling_algorithm_duration:p90
            expr: histogram_quantile(0.90, sum by (le) (rate(scheduler_scheduling_algorithm_duration_seconds_bucket[1m])))

          - record: koord:scheduler_scheduling_algorithm_duration:p99
            expr: histogram_quantile(0.99, sum by (le) (rate(scheduler_scheduling_algorithm_duration_seconds_bucket[1m])))

          # Framework extension point latency
          - record: koord:scheduler_framework_extension_point_duration:p90
            expr: histogram_quantile(0.90, sum by (le) (rate(scheduler_framework_extension_point_duration_seconds_bucket[1m])))

          # Plugin execution latency
          - record: koord:scheduler_plugin_execution_duration:p90
            expr: histogram_quantile(0.90, sum by (le) (rate(scheduler_plugin_execution_duration_seconds_bucket[1m])))

      # ──────────────────────────────────────────────────
      #  Success rate
      # ──────────────────────────────────────────────────
      - name: koord_success
        interval: 15s
        rules:
          - record: koord:scheduler_schedule_attempts:success_rate1m
            expr: >
              sum(rate(scheduler_schedule_attempts_total{result="scheduled"}[1m]))
              /
              sum(rate(scheduler_schedule_attempts_total[1m]))

          - record: koord:scheduler_schedule_attempts:error_rate1m
            expr: >
              sum(rate(scheduler_schedule_attempts_total{result="error"}[1m]))
              /
              sum(rate(scheduler_schedule_attempts_total[1m]))
