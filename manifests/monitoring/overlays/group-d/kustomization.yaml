apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Group D — Volcano (CNCF batch scheduler reference)
# Usage: kubectl apply -k manifests/monitoring/overlays/group-d/

resources:
  - ../../base
  - volcano-services.yaml
  - prometheus-config.yaml
